version: '2'

services:
  web:
    build: ./server
    command: python run.py
    ports:
      - "80:5555"
    env_file: .env
    volumes:
      - .:/server
    links:
      - db 

  db:
    image: jareechang/postgresql:9.4 
    ports: 
      - "50:5432"
